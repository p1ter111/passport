# Passport cover assets

This directory contains 199 Passport Atlas cover illustrations generated by
`npm run data:refresh`.

The files are original, non-official visual representations for this product.
They are not scans, replicas, identity documents, or substitutes for official
passport imagery. Each cover uses the country's native name, localized passport
label, ISO identifiers, a data-driven color, and an original emblem motif.
